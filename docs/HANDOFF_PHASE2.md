# Передача Фази 2 — 14.09.2026

## Завдання Андрія

Продовжувати Andriithis/moya-vulytsya за ROADMAP.md, зараз Фаза 2 — місце події
та геокодування. UX уже прийнятий, не переглядати. Особливий захист від
підміни EVENT_LOCATION адресою суду, установи, проживання, роботи або майна.
Працювати більшими завершеними блоками: branch → tests → PR → green CI → merge
безпечних змін. Не питати дозволу на кожну технічну дію. ROADMAP відмічати
лише після реальної перевірки. Пояснювати українською простою мовою.

## Уже злитий код

PR #4: https://github.com/Andriithis/moya-vulytsya/pull/4
main: 9ed73750af949e50bd2011c0d1325e0f746e4567 (перевірено 14.09.2026).
Витягувач location-roles-v1, контекст/ролі адрес, SQLite address_evidence,
спільний допуск геокодера й карти; старі адреси без evidence → UNKNOWN.
36 тестів тоді пройшли (33 unit + 3 PostgreSQL/PostGIS).

## Поточна НЕЗАВЕРШЕНА гілка

`codex/phase2-durable-extraction`. Це передача роботи, не готовий реліз.

- Додано `document_extraction` у схему та міграцію 0003: приватний checkpoint
  із source_row_hash, text_sha256, версією, сумісним рядком і кандидатами.
- `pipeline/extraction_store.py`: перевірка результату, запис checkpoint
  і завершення черги в одній транзакції з блокуванням document; відновлення
  лише для поточної активної версії документа, відхилення старих доказів.
- Черга PostgreSQL повертає source_row_hash і повторно бере документи без
  checkpoint поточної версії. Step1 відновлює контексти з PostgreSQL, записує
  хеші, не завершує помилки завантаження як успіх. Додано rollback SQLite
  і припинення після помилки потоку. Без DATABASE_URL лишається локальний режим.
- location-roles-v2: адреса перед дією, м. Київ у прив'язці, правильні межі
  речень біля скорочень та КУпАП. Негативні ролі мають пріоритет.
- QA: `python -m pipeline.location_qa`, маніфест data/fixtures/location_qa.json.
  Використано 13 уривків із ВЖЕ наявного FABULA_REZULTAT.txt; нові повні тексти
  не додавалися. Посилання й SHA256 фіксують джерела. Розмітку зробив асистент.

## Фактичні перевірки на момент передачі

14.09.2026 локально: unittest — 52 знайдено, 43 пройшли, 9 PostgreSQL-тестів
пропущені без TEST_DATABASE_URL. CI нового блоку ще потрібно перевірити.
На Windows для тимчасових каталогів застосовано зовнішній runner з успадкованими
ACL; код тестів у репозиторії не підмінявся. У CI запускати звичайний unittest.

QA уривків: 10 очікуваних місць із 13 прикладів; 8 знайдено правильно,
0 неправильних, 2 пропущено (761/34866/24, 758/10578/24). Це розробницький
набір, використаний під час змін, НЕ незалежна оцінка production precision.
Попередня v1 не підтверджувала жодної адреси на всіх 56 кешованих уривках.
v2 підтвердила 9/56, але розмічено лише вказані 13, не всі 56.

## Що робити наступним

1. Прочитати AGENTS.md, ROADMAP.md, поточний diff і цей файл. Перевірити main/PR.
2. Довести draft до готовності: рев'ю, CI PostgreSQL/PostGIS, потрібні виправлення.
   Особливо перевірити атомарність, помилки потоку, stale source, відновлення в
   порожню SQLite, повторний запуск, відсутність хибного завершення черги.
3. Перевірити/допрацювати зв'язок source_row_hash у postgres_events sync:
   зараз міст читає кандидатів із локальної SQLite; потрібна гарантія, що
   пізніша зміна document не дозволить прив'язати застаріле EVENT_LOCATION.
4. Описати порядок міграції 0003 та режим DATABASE_URL. Приватний production
   PostgreSQL і secrets у цій сесії НЕ налаштовані; weekly workflow не активований
   на нове сховище. Не стверджувати, що production weekly вже відновлено.
5. Оновити docs/08-stan.md та документацію v1/v2; ROADMAP змінювати лише за
   завершеними перевіреними підпунктами. Тоді ready PR → green CI → merge.
6. Далі якість геокодування: старий step2 досі має centroid/fuzzy-tail fallback,
   немає надійного порога, повної перевірки адміністративної території та
   перевіреної нормалізації перейменованих вулиць. Це ще відкрита Фаза 2.

## Межі й доступ

Не скрапити reyestr.court.gov.ua. Офіційні посилання на RTF в уривках ведуть
на od.reyestr.court.gov.ua/files; під час цього блоку їх повторно не завантажували.
Не видавати кешовані уривки за повні рішення чи людський QA. Контексти й SQLite
не публікувати в Git/публічних Actions artifacts. Нові production платежі,
хостинг, юридичні рішення не обрані. Фаза 2 загалом не завершена.
